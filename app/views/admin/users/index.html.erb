<%= link_to "Back to all movies", movies_path %> <br />

<h1>List of Users</h1>
<%= paginate @users %>

<% if @users %>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to "#{user.full_name}", admin_user_path(user) %></li>
    <% end %>  
  </ul> 
<% end %>